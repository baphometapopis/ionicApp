import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.baphomet.push',
  appName: 'sample',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
